%h2= t :'.title'

%p
  = paginate @events

%table.table
  %thead
    %tr
      %th #
      - %i( name properties visit user time ).each do |att|
        %th= Ahoy::Event.human_attribute_name(att)
  %tbody
    - @events.each do |event|
      %tr
        %td= link_to short_uuid(event.id), admin_event_path(event)
        %td= event.name
        %td
          %ul
            - event.properties.each do |key, value|
              %li= "#{key.capitalize}: #{value}"
        %td
          - if event.visit
            = link_to short_uuid(event.visit.id), admin_visit_path(event.visit)
          - else
            &empty;
        %td
          - if event.user
            = link_to event.user.name, admin_user_path(event.user)
          - else
            &empty;
        %td= l event.time, format: :digits

%p
  = paginate @events
